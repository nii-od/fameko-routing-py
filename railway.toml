[build]
builder = "nixpacks"

[build.nixpacks]
providers = ["python"]

[deploy]
startCommand = "uvicorn routing_service:app --host 0.0.0.0 --port 8080"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
